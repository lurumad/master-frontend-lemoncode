import { KittiesContainer } from "@/pods/kitties";
import { AppLayout } from "@/layouts";

export const KittiesPage = () => {
  return (
    <AppLayout>
      <KittiesContainer />
    </AppLayout>
  );
};
