import { AppLayout } from "@/layouts";
import { PuppiesContainer } from "@/pods/puppies";

export const PuppiesPage = () => {
  return (
    <AppLayout>
      <PuppiesContainer />
    </AppLayout>
  );
};
